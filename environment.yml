# scQPAS Conda Environment
# Install with: conda env create -f environment.yml

name: scqpas
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.10
  - pandas>=1.5.0
  - numpy>=1.23.0
  - pysam>=0.21.0
  - samtools
  - bedtools